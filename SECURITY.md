# Security policy

Report package parser, path validation, hash validation, or unsafe bundle-load
issues through a private GitHub security advisory when that feature is
available. Do not attach proprietary OPERATOR files, private logs, user paths,
or access tokens to a public issue.

This project accepts only data from Core-validated package roots. A change
must preserve canonical path checks, file closure, hash checks, exact scene
identity, and fail-closed behavior.
